#!/usr/bin/env bash
# bin/release <build-dir>

#echo "hi!"
#rootdir=$PWD
#cd /etc/
#ls
# cd lib/db/default_tables/mysql2postgres
# psql ${array[3]} < gidget.dump
#cd $rootdir
# psql ${array[3]} < gidget.dump

cat <<EOF
---
addons: []
default_process_types:
  web: php -S 0.0.0.0:\$PORT
EOF
